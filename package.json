{
  "name": "cider-searchable-meetings",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply cider-searchable-meetings --local",
    "d1:migrate:remote": "wrangler d1 migrations apply cider-searchable-meetings --remote",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260520.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.16.0"
  }
}
